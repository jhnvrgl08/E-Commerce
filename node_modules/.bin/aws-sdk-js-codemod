#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../aws-sdk-js-codemod/bin/aws-sdk-js-codemod" "$@"
else 
  exec node  "$basedir/../aws-sdk-js-codemod/bin/aws-sdk-js-codemod" "$@"
fi
